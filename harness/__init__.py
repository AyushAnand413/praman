"""Test harnesses — the buyer side and the measurement rigs.

Nothing here is part of the merchant system; it is the counterparty and the
instrumentation.

Modules: grahak.py — the buyer agent (wallet signs a mandate → discovery →
catalog → offer → checkout); and the 400-session A/B harness, run in shadow
mode.
"""
